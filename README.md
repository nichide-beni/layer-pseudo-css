# 擬似要素とcss layerを使った戦略的なcss構築

## :isと:where

:is, :whereともに動きはほぼ同じで、引数に指定したセレクタに該当したもの全てにスタイルを適用する。  
両者の違いは、引数の結果の詳細度が:isは引数で最も高い詳細度に引っ張られ、:whereでは-になる。  

### :is, :has両者の使い分け

- :isは通常使用をするとき
- :whereはライブラリや、上書きを前提として使用するとき

### 書き方

:is(セレクター1, セレクター2)   {}
:where(セレクター1, セレクター2) {}

### :hasによる別のセレクタを使った記述

これまで実現できなかった「特定の子要素を兄弟を持つ要素」を指定できるようになった。  
つまり、後で記述する要素によって先に記述された要素にスタイルを当てることができる。  

### sample01 擬似セレクタの応用

擬似セレクタの:is, :where, :hasをそれぞれ使ったサンプル。  
isやwhereを使ってフォーム部品や見出しなど共通で設定するようなスタイルを設定し、hasではチェック状態や親子関係の有無など柔軟な状態判定が可能。  

- whereは上書きしやすいようにベースのスタイルとして
- isはセレクタとして使用箇所を想定した形式で使用(通常はこちらを採用する可能性が高い)
- hasは汎用性が高く、JavaScriptで判定していたように状態によってスタイルを切り分ける。

## レイヤーによる優先順位の制御

cssの@layerでレイヤー順を指定すると、詳細度をよりも優先してスタイルを適用することができる。  
上書きされやすい共通スタイルなどを上層のレイヤーで宣言し、個別のコンテンツスタイルを後から宣言する。  
ライブラリをそのまま読み込むとレイヤー外の強いスタイルになる可能性があるため、cssでimportするなどの工夫が必要。  

### レイヤーの優先順位

@layer layer1, layer2, layer3, ..., と宣言した場合、後で宣言したlayer3で宣言したスタイルが適用される。  
あくまでスタイル単位の適用になるため、layer1でのみ宣言したスタイルはそのまま適用される。  

#### importantの優先順位

importantの場合はレイヤーの宣言順(先が優先)される仕様になっているため注意が必要。  
上書きをしたい場合は、レイヤー外に宣言する必要がある。  
レイヤーの詳細: https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@layer  

#### レイヤーの優先順位まとめ

- 通常のスタイル(優先順)
 - レイヤーの外で宣言されたスタイル
 - レイヤー宣言の逆順、後で宣言したレイヤーが優先
- important指定によるスタイル(優先度)
 - レイヤーの外で宣言されたスタイル
  - レイヤー宣言順、先に宣言したレイヤーが優先

### sample02 レイヤーによる優先順位の使用例

通常スタイルとimportantのスタイルを織り交ぜつつ、レイヤーの適用状況を確認する。  

## example01 演習

デザインは添付の画像を参照すること。  
指定されていないエリアについては差があってもいいものとする(ピクセルパーフェクトの意識は不要)。  

### 課題の要件

【課題】モダンCSSを活用した「山と自然の紹介シングルページ」の実装提供されたデザインカンプ（※本コードから起こしたデザイン）を元に、最新のCSS仕様を駆使してセマンティックかつクリーンなコードでWebサイトを構築してください。  
記入箇所には「TODO:」を振っているため、コメントを参考にしてください。ただし、TODOだけを見ても解けないと思うので要件とコードの確認も必要になると思います。  

#### cssレイヤー要件

- @layer を用いて優先度を明確に分離すること。レイヤーの優先順位は低い方から reset, base, contents と定義すること。
- リセットCSS（ress.cssなど）は layer(reset) 属性、または @import を使用して最下位レイヤーとして読み込むこと。レイヤーによる管理を前提とし、破
- 綻を防ぐため4階層以上のネスト（疑似要素除く）や、長すぎるクラス名は禁止とする。

#### HTML要件

- HTMLは記述済みのため変更の必要がないが、適宜調整しても良い

#### スタイル要件

- 計算基礎と変数によるレスポンシブ設計html 要素のフォントサイズを調整し、1rem = 10px 相当として計算できるようにすること。
- 以下の要素は、CSS変数（カスタムプロパティ）を用いて定義し、メディアクエリ（ブレイクポイント 1024px）で変数の値を切り替えること。
 - body のフォントサイズ（SP: 14px / PC: 16px）
 - 左右のインライン余白（SP: 1rem / PC: 3rem）
 - メインコンテンツの最大横幅は、左右の余白を含まない純粋なコンテンツ幅として 100rem（1000px相当） とすること。
 
#### UI要件

- メインビジュアル（MV）の画像制御MVは全画面（100vh）表示とする。
- 背景画像（background-image）の使用は禁止とし、HTMLの <img> タグに対して object-fit を適用して画面いっぱいに表示させること。
- Bento UI（ギャラリー）のGrid Area制御写真ギャラリーは display: grid と grid-template-areas を使用して、以下のグリッド構造（Bento UI）を構築すること。
- SP版（2列構成）
 - 1行目: moss (11), pebble (11)
 - 2行目: creek (11), valley (11)  
 - ※PCの一番下の行から持ってくる3行目: main (2*2の幅いっぱいに広がる)
- PC版（4列構成・ブレイクポイント1024px）
 - 1行目: moss (11), pebble (11),
 - main (2*2のパーツの上半分)2行目: creek (11),
 - valley (11), main (2*2のパーツの下半分)
 
#### セレクタ要件

- モダン疑似クラス（:is, :where, :has）によるスタイル制御
 - :is() の活用: 案内カード（.intro-card）にホバーした際、内部の見出しと本文のテキスト色を同時に1つのセレクタで変更すること。
 - :where() の活用: バッジ要素の初期スタイルを :where() で記述し、詳細度を 0 にリセットすること。その後、通常のクラスセレクタを用いて詳細度の競合を起こさずに背景色を上書きできるか検証すること。
 - :has() の活用: 特定のバッジ（.badge-important）を内部に持っている親要素（.intro-card）の枠線と背景色を自動で変更する「親要素の逆引き制御」を実装すること。
